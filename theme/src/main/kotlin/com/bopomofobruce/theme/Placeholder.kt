package com.bopomofobruce.theme

/**
 * Skeleton marker for the `:theme` module.
 *
 * W1-B (`feat/w1-theme`) implements the real theme engine: Material You + light/dark themes and the
 * photo background pipeline. Until then this constant exists so the module has at least one Kotlin
 * source.
 */
internal const val MODULE_NAME: String = "theme"
