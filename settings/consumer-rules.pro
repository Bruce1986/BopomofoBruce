# Consumer ProGuard rules for the :settings module.
#
# Empty by design at W0-1 skeleton — no module-specific shrink rules needed yet.
# Downstream Wave 實作該 module 時再加：keep rules、optimization hints 等。
