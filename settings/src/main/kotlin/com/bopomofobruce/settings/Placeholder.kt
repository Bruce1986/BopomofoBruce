package com.bopomofobruce.settings

/**
 * Skeleton marker for the `:settings` module.
 *
 * W2-C (`feat/w2-settings`) implements `SettingsActivity`, the FirstRun activity, the Compose
 * pages, and DataStore-Proto persistence. Until then this constant exists so the module has at
 * least one Kotlin source.
 */
internal const val MODULE_NAME: String = "settings"
