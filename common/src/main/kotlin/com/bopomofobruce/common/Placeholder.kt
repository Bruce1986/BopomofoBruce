package com.bopomofobruce.common

/**
 * Skeleton marker for the `:common` module.
 *
 * W0-2 (`feat/w0-common-contracts`) will replace this with the real interface contracts (KeyData,
 * Candidate, InputState, ZhuyinDecoder, KeyboardTheme, etc.). Until then this constant exists so
 * `:common` has at least one Kotlin source and downstream modules can compile against it.
 */
internal const val MODULE_NAME: String = "common"
