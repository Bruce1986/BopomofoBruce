package com.bopomofobruce.decoder

/**
 * Skeleton marker for the `:decoder` module.
 *
 * W2-A (`feat/w2-decoder-jni`) implements the JNI binding wrapping `libbpmf.so`, the personal
 * dictionary (Room), and `ZhuyinDecoder` integration. Until then this constant exists so the module
 * has at least one Kotlin source.
 */
internal const val MODULE_NAME: String = "decoder"
