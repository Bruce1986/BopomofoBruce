package com.bopomofobruce.ime

/**
 * Skeleton marker for the `:ime` module.
 *
 * W2-B (`feat/w2-ime-service`) implements `BpmfInputMethodService`, the Compose IME view, the
 * candidate row, and the processor-chain runtime. Until then this constant exists so the module has
 * at least one Kotlin source.
 */
internal const val MODULE_NAME: String = "ime"
