package com.bopomofobruce.keyboards

/**
 * Skeleton marker for the `:keyboards` module.
 *
 * W1-C (`feat/w1-keyboards`) implements the real KeyboardDef definitions (注音 4x10, symbols,
 * numeric, password, etc.). Until then this constant exists so the module has at least one Kotlin
 * source.
 */
internal const val MODULE_NAME: String = "keyboards"
