package com.bopomofobruce.app

import android.app.Application

/**
 * Placeholder Application class so `:app` can produce a valid debug APK on the W0 skeleton.
 *
 * W3-1 (`feat/w3-app-wiring`) will replace this with `BpmfApplication`, the manual-DI graph, and
 * registration of `BpmfInputMethodService` in the Manifest.
 */
class BpmfPlaceholderApplication : Application()
